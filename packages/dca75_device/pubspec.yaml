name: dca75_device
description: Typed DCA75 client, connection controller (state machine, polling, drafts) and curve-sweep engine. Pure Dart; testable against fake transports.
version: 0.1.0
publish_to: none
resolution: workspace
environment:
  sdk: ^3.6.0
dependencies:
  dca75_protocol: ^0.1.0
  dca75_transport: ^0.1.0
  meta: ^1.15.0
dev_dependencies:
  fake_async: ^1.3.0
  lints: ^6.0.0
  test: ^1.25.0
