name: dca75_store
description: SQLite datastore (drift) for DCA75 readings, sweeps, parts and bins.
version: 0.1.0
publish_to: none
resolution: workspace
environment:
  sdk: ^3.6.0
dependencies:
  dca75_device: ^0.1.0
  dca75_protocol: ^0.1.0
  drift: ^2.32.0
  sqlite3: ^3.0.0
  path: ^1.9.0
dev_dependencies:
  build_runner: ^2.4.0
  drift_dev: ^2.32.0
  lints: ^6.0.0
  test: ^1.25.0
