name: dca75_cli
description: Command-line tools for the DCA75 Workbench - decode frames, replay captures, seed databases.
version: 0.1.0
publish_to: none
resolution: workspace
environment:
  sdk: ^3.6.0
dependencies:
  args: ^2.5.0
  dca75_protocol: ^0.1.0
  dca75_device: ^0.1.0
  dca75_transport: ^0.1.0
dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
